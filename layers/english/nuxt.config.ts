/**
 * Nuxt layer: english.
 *
 * File nuxt.config.ts phải tồn tại (dù rỗng) để folder được coi là 1 Nuxt layer hợp lệ.
 * Layer tự đóng gói: app/, server/api/, stores/. Root nuxt.config.ts KHÔNG cần khai báo
 * — Nuxt 4 auto-scan `layers/*`.
 */
export default defineNuxtConfig({})
