<script setup lang="ts">
/**
 * Auth layout — dùng cho /login, /register, /forgot, /reset.
 * Blank shell: page tự dùng `<AuthShell>` component để vẽ gradient + card.
 */
</script>

<template>
  <slot />
</template>
